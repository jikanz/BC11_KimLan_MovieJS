const initialState = {
  movies: [],
  isLoading: false,
  error: null,
};

const movieManagementReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default movieManagementReducer;
